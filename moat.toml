# The changelog workflow pushes CHANGELOG.md straight to the release branch as a verified,
# GitHub-API-signed commit, so the "require a pull request" rule is intentionally dropped on
# this repo. Silence the matching Moat check; signed-commit, force-push and deletion
# protection all remain enforced.
[checks]
repositories_pull_requests_require_reviews = "off"
